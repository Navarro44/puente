// Shared types, enums, ABIs, and external-dependency interfaces for Puente.
// Both apps/web and apps/oracle import from this package.
// Per the "interfaces over integrations" principle, every external dependency
// (carrier APIs, KYB providers, ramp partners, sanctions screening) must be
// defined as a TypeScript interface here before any implementation is written.

export {};
